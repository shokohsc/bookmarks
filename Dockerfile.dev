FROM node:22-alpine
WORKDIR /app
RUN addgroup -S bookmarks && adduser -S bookmarks -G bookmarks
COPY package.json package-lock.json* ./
RUN npm ci && npm cache clean --force
COPY --chown=bookmarks:bookmarks . .
USER bookmarks
EXPOSE 8001
CMD ["npm", "run", "dev"]
